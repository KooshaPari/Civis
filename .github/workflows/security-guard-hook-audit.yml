name: Security Guard (Hooks)

# Local-first: PRs use quality.yml + pr-governance-gate only.
# Run heavy gates locally: lefthook run pre-push
# phenoShared reusable is 404; replaced with no-op.
on:
  workflow_dispatch:
  push:
    branches: [main]
permissions:
  contents: read

jobs:
  # TODO: Implement custom security guard hook audit logic.
  guard:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - run: echo "TODO: replace with custom security-guard-hook-audit logic"
