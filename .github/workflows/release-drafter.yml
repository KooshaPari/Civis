name: Release Drafter

# Placeholder until a maintained release-drafter implementation is restored.
# Keep it manual so a no-op cannot create a false failing main check.
on: workflow_dispatch

permissions:
  contents: read

jobs:
  # phenoShared reusable is 404; replaced with no-op.
  # TODO: Implement custom release drafting logic.
  release-drafter:
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      pull-requests: write
    steps:
      - run: echo TODO-release-drafter-placeholder
