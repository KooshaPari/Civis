name: self-merge-gate

# phenoShared reusable is 404; replaced with no-op.
# TODO: Implement custom self-merge gate logic.

on: [pull_request_review]

permissions:
  contents: read

jobs:
  gate:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'TODO: replace with custom self-merge-gate logic'
