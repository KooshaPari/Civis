---
title: Feature Proof
description: Autonomous visual evidence that DINOForge features work
layout: home
---

# Feature Proof

Autonomous visual evidence that all DINOForge features work. This page is auto-generated by `/prove-all` and updated per-commit with the latest evidence bundle.

## Latest Run

<script setup>
import { ref, onMounted } from 'vue'

const latestMetadata = ref(null)
const hasBundle = ref(false)
const bundleFiles = ref({})

onMounted(async () => {
  try {
    // Try to load bundle metadata
    const response = await fetch('/proof/latest/bundle_metadata.json')
    if (response.ok) {
      latestMetadata.value = await response.json()
      hasBundle.value = true

      // Check which files exist
      const filesToCheck = [
        'raw_mods.mp4', 'raw_f9.mp4', 'raw_f10.mp4',
        'validate_mods.png', 'validate_f9.png', 'validate_f10.png',
        'mods_feature.mp4', 'f9_feature.mp4', 'f10_feature.mp4', 'dinoforge_reel.mp4',
        'proof_report.md', 'validate_report.json'
      ]
      for (const file of filesToCheck) {
        try {
          const res = await fetch(`/proof/latest/${file}`, { method: 'HEAD' })
          bundleFiles.value[file] = res.ok
        } catch (e) {
          bundleFiles.value[file] = false
        }
      }
    }
  } catch (e) {
    console.log('No bundle available yet')
  }
})
</script>

<div v-if="latestMetadata" class="bundle-info">

**Timestamp**: {{ latestMetadata.generated_at ? new Date(latestMetadata.generated_at).toLocaleString() : 'Unknown' }}

**Pipeline Version**: {{ latestMetadata.pipeline_version || 'Unknown' }}

</div>

<div v-else class="pending-info">

**Status**: No proof bundle available yet. Run `/prove-all` to generate one.

</div>

---

## Validation Results

| Feature | VLM Confirmed | Screenshot | Raw Clip |
|---------|:---:|:---:|:---:|
| Mods Button | {{ bundleFiles['validate_mods.png'] ? '✓' : '—' }} | [✓](./latest/validate_mods.png) | [✓](./latest/raw_mods.mp4) |
| F9 Debug Overlay | {{ bundleFiles['validate_f9.png'] ? '✓' : '—' }} | [✓](./latest/validate_f9.png) | [✓](./latest/raw_f9.mp4) |
| F10 Mod Menu | {{ bundleFiles['validate_f10.png'] ? '✓' : '—' }} | [✓](./latest/validate_f10.png) | [✓](./latest/raw_f10.mp4) |

---

## Rendered Videos

### Feature Reel

<div v-if="bundleFiles['dinoforge_reel.mp4']" class="video-container">
<video controls width="100%" style="max-width: 800px; border-radius: 8px;">
  <source src="/proof/latest/dinoforge_reel.mp4" type="video/mp4">
  Your browser does not support the video tag.
</video>
</div>

<div v-else class="pending-info">

**Status**: Feature reel not yet rendered. Run `/prove-all` to generate.

</div>

### Individual Feature Clips

#### Mods Button Feature

<div v-if="bundleFiles['mods_feature.mp4']" class="video-container">
<video controls width="100%" style="max-width: 600px; border-radius: 8px;">
  <source src="/proof/latest/mods_feature.mp4" type="video/mp4">
  Your browser does not support the video tag.
</video>
</div>

<div v-else class="pending-info">

**Status**: Pending render

</div>

#### F9 Debug Overlay Feature

<div v-if="bundleFiles['f9_feature.mp4']" class="video-container">
<video controls width="100%" style="max-width: 600px; border-radius: 8px;">
  <source src="/proof/latest/f9_feature.mp4" type="video/mp4">
  Your browser does not support the video tag.
</video>
</div>

<div v-else class="pending-info">

**Status**: Pending render

</div>

#### F10 Mod Menu Feature

<div v-if="bundleFiles['f10_feature.mp4']" class="video-container">
<video controls width="100%" style="max-width: 600px; border-radius: 8px;">
  <source src="/proof/latest/f10_feature.mp4" type="video/mp4">
  Your browser does not support the video tag.
</video>
</div>

<div v-else class="pending-info">

**Status**: Pending render

</div>

---

## Desktop Companion UI

<div class="pending-info">

**Status**: Desktop Companion video not yet available. Run `/prove-all` with Playwright tests enabled.

</div>

---

## CLI Recordings

### CLI Help

<div v-if="bundleFiles['cli_help.gif']" class="image-container">
![CLI Help](/proof/latest/cli_help.gif)
</div>

<div v-else class="pending-info">

**Status**: CLI recording not yet captured. Run `/prove-all` with VHS installed.

</div>

### Pack Validate

<div v-if="bundleFiles['pack_validate.gif']" class="image-container">
![Pack Validate](/proof/latest/pack_validate.gif)
</div>

### Pack Build

<div v-if="bundleFiles['pack_build.gif']" class="image-container">
![Pack Build](/proof/latest/pack_build.gif)
</div>

---

## Detailed Report

<div v-if="bundleFiles['proof_report.md']" class="report-link">

**Full Report**: [proof_report.md](./latest/proof_report.md)

</div>

<div v-else class="pending-info">

**Status**: Detailed report will be generated on next `/prove-all` run.

</div>

---

## Raw Evidence Files

<div v-if="hasBundle" class="evidence-table">

| File | Type | Available |
|------|------|-----------|
| raw_mods.mp4 | Raw clip | {{ bundleFiles['raw_mods.mp4'] ? '✓' : '—' }} |
| raw_f9.mp4 | Raw clip | {{ bundleFiles['raw_f9.mp4'] ? '✓' : '—' }} |
| raw_f10.mp4 | Raw clip | {{ bundleFiles['raw_f10.mp4'] ? '✓' : '—' }} |
| validate_mods.png | Screenshot | {{ bundleFiles['validate_mods.png'] ? '✓' : '—' }} |
| validate_f9.png | Screenshot | {{ bundleFiles['validate_f9.png'] ? '✓' : '—' }} |
| validate_f10.png | Screenshot | {{ bundleFiles['validate_f10.png'] ? '✓' : '—' }} |
| validate_report.json | Validation report | {{ bundleFiles['validate_report.json'] ? '✓' : '—' }} |
| proof_report.md | Markdown report | {{ bundleFiles['proof_report.md'] ? '✓' : '—' }} |
| bundle_metadata.json | Bundle metadata | {{ bundleFiles['bundle_metadata.json'] ? '✓' : '—' }} |

</div>

---

## Methodology

The evidence in this directory is produced by the `/prove-all` command, which runs an autonomous pipeline:

1. **Game Capture (Phase 1)** — Records raw video clips of three core features:
   - Mods button visibility on main menu
   - F9 debug overlay with runtime stats
   - F10 mod menu panel
   - Each clip is validated by VLM (computer vision) to confirm the UI element is visible

2. **Neural TTS (Phase 2)** — Generates narration using Microsoft Edge's neural voice (edge-tts)

3. **Remotion Composition (Phase 3)** — Renders videos with:
   - Spring-physics callout boxes
   - Narration audio synced to visuals
   - Feature transitions and branding

4. **Desktop Companion (Phase 4)** — Records UI flow video (if Playwright tests configured)

5. **CLI Recordings (Phase 5)** — Captures terminal sessions showing CLI tool usage (if VHS installed)

6. **Bundling (Phase 6-8)** — Copies all artifacts to `docs/proof/latest/`, updates this page, and commits

### Technical Details

- **Window Capture**: GDI title-based capture (title="Diplomacy is Not an Option")
- **Key Injection**: Win32 SendInput (no window focus required)
- **VLM Validation**: claude-haiku-4-5-20251001 analyzing live GDI screenshots
- **TTS**: edge-tts en-US-AriaNeural (free neural voice)
- **Rendering**: Remotion v4 with FFmpeg output

---

## Running Your Own

To generate fresh proof:

```bash
/prove-all
```

This command requires:
- Game installed at `G:\SteamLibrary\steamapps\common\Diplomacy is Not an Option\`
- DINOForge DLL deployed and loading (F10 menu works, no error overlays)
- Python 3 + edge-tts: `pip install edge-tts`
- Node.js 18+ with Remotion: `cd scripts/video && npm install`
- ffmpeg: `C:\program files\imagemagick-7.1.0-q16-hdri\ffmpeg.exe`

The entire pipeline takes ~10-15 minutes depending on Remotion render times.

<style scoped>
.bundle-info {
  background: rgba(74, 222, 128, 0.1);
  border-left: 4px solid #4ade80;
  padding: 1rem;
  border-radius: 4px;
  margin: 1rem 0;
  font-size: 0.95rem;
}

.pending-info {
  background: rgba(241, 196, 15, 0.1);
  border-left: 4px solid #fbbf24;
  padding: 1rem;
  border-radius: 4px;
  margin: 1rem 0;
  font-size: 0.95rem;
}

.video-container {
  margin: 2rem 0;
  display: flex;
  justify-content: center;
}

.image-container {
  margin: 1.5rem 0;
  text-align: center;
}

.image-container img {
  max-width: 100%;
  border-radius: 8px;
  box-shadow: 0 2px 12px rgba(0, 0, 0, 0.3);
}

.evidence-table {
  margin: 1rem 0;
}

.report-link {
  background: rgba(59, 130, 246, 0.1);
  border-left: 4px solid #3b82f6;
  padding: 1rem;
  border-radius: 4px;
  margin: 1rem 0;
}
</style>
