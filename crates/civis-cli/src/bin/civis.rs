//! Umbrella entrypoint for the `civis` command family.
//!
//! The workspace manifest declares this binary so downstream tooling can
//! depend on a stable `civis` executable name even when the more specialized
//! `civis-*` helpers are used directly.

fn main() {}
