# VitePress Workflow (Bun)

From `../civ/docs`:

1. Install: `bun install`
2. Dev server: `bun run dev`
3. Build static site: `bun run build`
4. Preview build: `bun run preview`

Output directory: `../civ/docs-dist`
