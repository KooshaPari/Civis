[package]
name = "civ-species"
description = "Phenotype expression: DNA → morphology + behavior weights; multi-species tagging"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
civ-genetics = { path = "../genetics" }
rand = "0.8.6"
rand_chacha = "0.3"

[dev-dependencies]
proptest = "1.11"
