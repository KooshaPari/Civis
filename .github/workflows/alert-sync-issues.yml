# Alert sync to issues. phenoShared reusable is 404; replaced with no-op.
# TODO: Replace with custom alert-sync logic when needed.
name: Alert sync issues
# Placeholder until custom alert synchronization is implemented.
# Keep it manual so the no-op cannot create a false scheduled/main failure.
on: workflow_dispatch

permissions:
  actions: read
  contents: read
  issues: write
  security-events: read

jobs:
  sync:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo 'TODO: replace with custom alert-sync logic'
