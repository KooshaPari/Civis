<Page
    x:Class="DINOForge.DesktopCompanion.Views.AssetBrowserPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:DINOForge.DesktopCompanion.Themes.Converters"
    xmlns:data="using:DINOForge.DesktopCompanion.Data"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="Asset Browser"
            Style="{StaticResource TitleTextBlockStyle}"
            Foreground="{StaticResource DFAccentBrush}" />

        <TextBlock
            Grid.Row="1"
            Text="Scans pack directories and lists discovered asset bundles."
            Style="{StaticResource DFSecondaryText}"
            VerticalAlignment="Center" />

        <ListView
            Grid.Row="2"
            x:Name="AssetList"
            ItemsSource="{x:Bind ViewModel.AssetGroups, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:PackAssetGroup">
                    <Border Style="{StaticResource DFCard}" Margin="0,4">
                        <StackPanel Spacing="8">
                            <TextBlock Text="{x:Bind PackName}" Style="{StaticResource DFBodyText}" FontWeight="SemiBold" />
                            <TextBlock Text="{x:Bind PackPath}" Style="{StaticResource DFSecondaryText}" />
                            <TextBlock Text="{x:Bind TotalSizeFormatted}" Style="{StaticResource DFSecondaryText}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>