# Spec Workflow

Every implementation change should trace back to a functional requirement,
specification section, ADR, or documented workstream item.

Before coding, identify the relevant spec surface. During implementation, update
traceability when behavior changes. Before merging, run the quality gates and
record the evidence in the appropriate worklog or PR.
