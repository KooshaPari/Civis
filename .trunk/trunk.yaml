# =============================================================================
# Trunk.io — Plugin versions for linting/formatting tools
# =============================================================================
# This file is auto-generated. Run `trunk upgrade` to update.
# https://docs.trunk.io/check/reference
# =============================================================================

version: 0.1

plugins:
  sources:
    - id: trunk
      ref: v1.5.0
      uri: https://github.com/trunk-io/plugins
    - id: configs
      ref: v1.0.4
      uri: https://github.com/trunk-io/configs

# Keep plugin-provided lint and format defaults enabled. Explicit tool names
# make Trunk CLI 1.22.2 synthesize SYSTEM versions instead of resolving the
# versions declared by the pinned plugin/config sources.
lint:
  disabled:
    - pylint
    - semgrep

cli:
  version: 1.22.2
