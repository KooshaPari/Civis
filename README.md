# Civis

Short status note: this README is a fleet-compliance scaffold created by the repo-steward
to satisfy the Phenotype Org baseline (see `AgilePlus/kitty-specs/eco-006-governance-sync`).

## What this repo is
- Stack: Rust (Cargo.lock present; root manifest pending)
- Domain: TBD — populate from source

## Build / Test
- `task validate | build | test | lint` (placeholders — no manifest yet)
- no build configured yet

## Spec / Worklog pointers
- `kitty-specs/` if a spec exists (none detected at scaffold time).
- `worklogs/` pointer when present (none detected at scaffold time).
