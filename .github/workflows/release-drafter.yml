name: Release Drafter

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  release-drafter:
    # SHA-pinned per #805 (was @main); main as of 2026-05-21
    uses: KooshaPari/phenoShared/.github/workflows/reusable/release-drafter.yml@136def14b63e59da150eae1dc525677746098dd0
    with:
      config-name: release-drafter.yml
    permissions:
      contents: write
      pull-requests: write
