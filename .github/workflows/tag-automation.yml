name: Tag Automation

# phenoShared reusable is 404; replaced with no-op.
# TODO: Implement custom tag automation logic.

on:
  push:
    tags:
      - 'v*'

jobs:
  tag:
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - run: echo "TODO: replace with custom tag-automation logic"
