# UI Re-verify Audit Report — Task #984

Date: 2026-05-31

## PRIORITIZED what actually needs fixing
1. **Ship a real prebuilt TMP font AssetBundle for menu text** (`assets/ui/sw_menu_font`) with `SW_MenuFont SDF` entry. `pack.yaml` points to this, but only `menu_font.ttf` exists; runtime cannot use raw TTF for DINO TMP atlas generation.
2. **Rebuild the 12 building bundle placeholders** under `packs/warfare-starwars/assets/bundles/*` that are currently 90-byte stubs, because build-panel visuals/icons will keep falling back to native art/state.
3. **Audit build-panel icon/frame skinning**: there is no active runtime code path that applies pack-specific build-panel chrome/icon sprites (`panel-frame`/`button` assets are not consumed), so add explicit surface coverage if a full build UI takeover is expected.

## Surface-by-surface status

| Surface | (a) Skinning code present in source? | (b) Required asset(s) + exists in pack | (c) Likely in-game state + why |
|---|---|---|---|
| Main menu | **Yes** (`MainMenuThemer.ApplyToMainMenu` does logo/bg/frame/title/font/theme resolution from `ui_theme`; auxiliary takeover also updates main menu states). | Requires `assets/ui/menu_logo.png` **present 158,883B**, `assets/ui/menu_bg.png` **present 421,409B**, `assets/ui/btn_normal.png` **present 781B**, `assets/ui/btn_hover.png` **present 2,478B**, `assets/ui/sw_menu_font` + `font_asset_name` (**missing**, only `assets/ui/menu_font.ttf` **34,180B** exists). | **Mostly skinned / partially degraded.** Visual takeover and button chrome are present (logo/background/button frames), but text-font replacement is effectively disabled due missing prebuilt TMP bundle. |
| Subpages + aux menus (settings/options/video/etc.) | **Yes** (`MainMenuThemer.ApplyToAuxiliaryMenus` + `CanvasReskinner.ReskinAllPages` + selector heuristics). | Same theme assets as main menu for chrome; `menu_logo`, `menu_bg`, `btn_normal`, `btn_hover` are present (`>200B`). Font still requires same missing `sw_menu_font` bundle for TMP text. | **Partially skinned / mostly functional, still legacy in places.** Known behavior shows AUX takeover applies bg+frames on 6 subpages, but text font remains native/unchanged and heuristic/reskin path is color-only for many controls. |
| MODS button (in native menus) | **Yes** (`NativeMenuInjector` injects/repurposes a clone button; `NativeUiHelper` preserves native visual transition behavior and text). | No pack-specific asset required; button is cloned from existing native button hierarchy. | **Native control with DINOForge injection overlay.** Looks native by deriving from cloned donor, but not pack-themed from `ui_theme`. |
| F10 mod panel | **Yes (open path only).** `ModMenuPanel` is built by DINOForge and wired to F10/Mods click, but it is its own UGUI panel (not a themed skin pass). | No `assets/ui` dependency required by runtime path; panel is assembled from `UiBuilder` primitives and builtin `Arial`-based text. | **Custom DINOForge panel, mostly unthemed.** It is UGUI and functional, but not skin-asset-driven like main menu takeover. |
| Loading screen | **Yes** (`LoadingScreenController` full-screen takeover path with pack theme scan and pack assets fallback). | Reads `assets/loading/loading-bg.png` **present 409,428B**, `assets/loading/loading-logo.png` **present 89,396B** (not under `assets/ui`, but `pack.yaml` supports under `loading_screen`). | **Skinned when total-conversion pack is active.** Uses pack theme background/logo, or falls back to built-in backdrop if load fails. |
| Build-panel icons / frames | **No dedicated, surface-specific handler** found in audited files; only `CanvasReskinner` does generic Selectable/Text color tints, and `MainMenuThemer` handles menus with button frame logic for menus/aux pages. | No runtime-used pack asset dependency in audited files for build panel icons. `menu` assets present (`btn_*`, `menu_*`) are not clearly mapped to build-panel icon surfaces; no `panel-frame`/unit/build icon bundle consumption here. | **Mostly native / partially recolored.** Generic control recolor may apply, but no explicit build-panel icon/chrome skin path is active in these surfaces. |
| Font (menu typography) | **Runtime loader exists but depends on prebuilt AssetBundle** (`MainMenuThemer.LoadPrebuiltFontAsset` + `ApplyFont`). `LoadingScreenController` intentionally uses builtin Arial due runtime constraints. | Required: `assets/ui/sw_menu_font` (bundle, expected >200B) + `SW_MenuFont SDF` entry. **Missing in pack** (only `assets/ui/menu_font.ttf`, 34,180B + license file). | **Known incomplete / native fallback.** Main-menu/TMP text font replacement is not happening; this is the highest-confidence failure currently. |