You are a git agent in repo C:/Users/koosh/Dino on branch wsm/agileplus-dag-20260610. A merge of origin/main is STAGED but UNCOMMITTED (.git/MERGE_HEAD exists, conflicts already resolved and staged). The lefthook commit-msg `conventional` hook is crashing on the commit with `sh -c: line 5: syntax error: unexpected end of file` — it breaks specifically when finalizing this merge commit, blocking the commit. This blocks PR #286 (wsm/agileplus-dag-20260610 → main) from becoming mergeable.

HARD RULES: NEVER --no-verify / LEFTHOOK=0 / git stash / push --force / reset --hard / rm. NEVER create worktrees. Respect the hook — FIX its root cause, don't bypass.

TASK:
1. Inspect lefthook.yml commit-msg `conventional` command and the generated .git/hooks/commit-msg. The hook does `MSG=$(cat "$1")` then `if echo "$MSG" | grep -qE '...'`. Diagnose why it throws `line 5: syntax error: unexpected end of file` ONLY on this merge commit (works on normal commits). Likely causes: lefthook not substituting the commit-msg file arg for merge commits, or the multi-line merge message (with `# Conflicts:` + backtick/special chars) breaking the `$(cat)` / `echo "$MSG"` expansion in lefthook's sh -c wrapper.
2. Fix the hook ROBUSTLY in lefthook.yml so it (a) reads the commit-msg file via lefthook's {1} template (not raw $1 if that's the bug), (b) only validates the FIRST line, (c) skips Merge/Revert commits, (d) parses cleanly as YAML (the regex contains ': ' which breaks unquoted inline YAML — use a block scalar `run: |`), (e) the shell is POSIX-clean so lefthook's `sh -c` wrapper doesn't choke. Test your hook logic standalone before relying on it. Run `lefthook validate` (must say All good) and `lefthook install --force` to regenerate hooks.
3. Finalize the merge: git commit the staged merge with a conventional message like `chore(merge): reconcile origin/main into integration branch (L2)`. Verify HEAD advanced and .git/MERGE_HEAD is gone and `git rev-list --count HEAD..origin/main` == 0.
4. Push: git -c push.recurseSubmodules=no push --no-recurse-submodules origin wsm/agileplus-dag-20260610 (lefthook pre-push runs full test suite ~5-9min; if it fails on a REAL test, fix; if flake/transport retry once). Verify git ls-remote origin refs/heads/wsm/agileplus-dag-20260610 == local HEAD.
5. Confirm PR #286 flips to MERGEABLE: gh pr view 286 --json mergeable,state.

REPORT to docs/sessions/consolidation-20260613/forge-L2-finalize-report.md: root cause of the hook bug, the exact fix applied, final HEAD sha, ls-remote verification, PR #286 mergeable state. Then echo: L2-DONE-<sha>.
