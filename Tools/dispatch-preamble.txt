=== Phenotype Shared Dispatch Preamble (Civis / cross-chat) ===
Include this fragment at the top of every dispatched worker prompt. It is shared
across the 5 Phenotype chats. Keep it terse; do not re-paste, reference it.

CHANNEL PRIORITY
- codex-spark is the PRIMARY worker channel (cheap, fast, high capacity):
    codex exec -m gpt-5.3-codex-spark --dangerously-bypass-approvals-and-sandbox --cd <REPO>
  Fall back to gpt-5.4-mini only on quota.
- Composer 2.5 (Tools/agent.ps1 -p) SPARINGLY — it drains a monthly cap and is
  pricier; reserve for fast-Opus-alt high-judgment work only.
- Reserve native Claude Agent() for judgment/vision tasks (reading screenshots).
- Avoid Sonnet/Haiku for code grind; they are weaker than codex-spark here.

WINDOWS / CODEX HARD RULES
- ALWAYS pass --dangerously-bypass-approvals-and-sandbox on Windows; plain
  workspace-write rejects the subprocess spawns cargo needs.
- codex silently no-ops inside a git worktree and hangs on a positional prompt:
  run it in the REAL repo dir and pipe the prompt via stdin, OR use Claude Agent
  isolation:worktree for parallel code lanes; never run codex in a linked worktree.

BUILD DISCIPLINE
- KILL competing procs FIRST: civ-standalone, civ-bevy-ref, civis, cargo, rustc.
- Each parallel lane gets a DISTINCT CARGO_TARGET_DIR on E: (C: fills) and a
  DISJOINT set of files — never two agents on the same file or target dir.
- Build green before reporting. Trust the exe artifact: an exit-255 with NO
  compiler error = a stale lock or cold cache; advise a rerun, do not "fix" it.
- Never `git stash`; commit dirty work to a WIP branch or keep working dirty.

VERIFY (vision-verify, non-negotiable)
- Telemetry PASS != visually correct. Capture a screenshot and READ the PNG
  yourself before claiming any visual fix. Assume broken until pixels confirm.
- Use `civis verify` (Tools/civis-cli) for the build->deploy->autoshot->assert loop.

QUALITY CHARTER
- wrap > hand-roll; reuse existing crates/forks first. Hexagonal + SOLID/DRY.
- xDD-first (tests/specs before impl). Reduce LOC, simplify. Trace to an FR/NFR.
