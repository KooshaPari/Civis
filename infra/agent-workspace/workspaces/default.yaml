workspace_id: civ-default
max_concurrency: 8
retry_policy:
  max_retries: 3
  backoff_seconds: [1, 2, 5]
budgets:
  global_eau_cap: 100000
  per_workflow_eau_cap: 10000
audit:
  require_trace_id: true
  require_event_envelope: true
