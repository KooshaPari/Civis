[package]
name = "civ-powers"
description = "Data-driven God-Tools power registry for Civis"
version.workspace = true
edition.workspace = true
authors.workspace = true
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[features]
default = []
