[package]
name = "voxel-bridge"
description = "Thin Civis-side ECS bridge over phenotype-voxel"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
bevy_ecs = "0.18"
bevy_math = "0.18"
# Pin the same git revision as crates/voxel so CI resolves one kernel without a sibling checkout.
phenotype-voxel = { package = "phenotype-voxel", git = "https://github.com/KooshaPari/phenotype-voxel", rev = "a4d115898c0db4afe77074fe51a302b1c09d4478" }
