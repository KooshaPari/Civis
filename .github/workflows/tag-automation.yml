name: Tag Automation

# phenoShared reusable is 404; replaced with no-op.
# TODO: Implement custom tag automation logic.

# Placeholder until custom tag automation is implemented.
# Keep it manual so the no-op cannot create a false release check.
on: workflow_dispatch

jobs:
  tag:
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - run: echo TODO-tag-automation-placeholder
