{
  "name": "civ-docs",
  "private": true,
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev --port 5199",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview --port 5199",
    "dev": "vitepress dev --port 5199",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "docs:test:unit": "node --test tests/unit/*.mjs",
    "docs:test:component": "node --test tests/component/*.mjs",
    "docs:test:e2e": "playwright test --config tests/playwright.config.ts --project=chromium",
    "docs:test": "bun run docs:test:unit && bun run docs:test:component && bun run docs:test:e2e",
    "docs:check": "bun run docs:build && bun run docs:test"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vue": "^3.5.0",
    "@playwright/test": "^1.49.0"
  }
}
