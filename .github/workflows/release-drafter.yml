name: Release Drafter

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  # phenoShared reusable is 404; replaced with no-op.
  # TODO: Implement custom release drafting logic.
  release-drafter:
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      pull-requests: write
    steps:
      - run: echo "TODO: replace with custom release-drafter logic"
