[package]
name = "civ-laws"
description = "Physics-law DB: versioned RON schema, conservation laws, futurism extension rules"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
ron = "0.12"
thiserror = "1"

[dev-dependencies]
proptest = "1.11"
